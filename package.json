{
  "name": "transifexify",
  "version": "1.0.6",
  "description": "a little script to remove undefined, null or empty values in a json file to avoid Transifex errors",
  "bin": {
    "transifexify": "bin/transifexify"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee -c lib",
    "prepublish": "npm run build"
  },
  "author": "maxlath",
  "license": "MIT",
  "dependencies": {
    "lodash.pick": "^3.1.0",
    "tiny-chalk": "^2.0.0"
  },
  "devDependencies": {},
  "keywords": [
    "transifex",
    "json"
  ]
}
